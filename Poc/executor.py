import json
def executor(data):
    if data:
        print("executor", data)









## goal of this function is to execute the command that is passed to it

#1. send message when trigger of threshold is applied.
#2.place Trade when Trigger is applied
#3.close Trade when trades for that particular symbol at second threshold
